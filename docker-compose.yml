services:
  s3-protosearch-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: s3-protosearch-dev
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - /sharepoint:/sharepoint:ro
      - /db:/db
